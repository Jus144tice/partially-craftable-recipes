# Normalize line endings: store text as LF in the repo.
* text=auto

# Scripts that must stay LF (run on the Linux CI runner / *nix shells).
gradlew text eol=lf
*.sh text eol=lf

# Windows batch scripts stay CRLF.
*.bat text eol=crlf

# Binary assets — never normalize.
*.jar binary
*.png binary
